{
  "name": "transaction-network-bfs",
  "version": "1.0.0",
  "description": "Educational BFS project for transaction networks.",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "benchmark": "node src/benchmark/run.js",
    "benchmark:queue": "node src/benchmark/runQueueStrategies.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
